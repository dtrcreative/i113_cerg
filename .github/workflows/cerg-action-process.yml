name: cerg-action-process
on: 
  push:
    branches: 
      - master

jobs:
  validating_docker:
    runs-on: self-hosted

    steps:
      - name: checkUser
        run : echo $USER
      - name: Docker version
        run : docker version
      - name: Docker ps
        run : docker ps   
