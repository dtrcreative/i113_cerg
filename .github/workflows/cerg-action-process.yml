name: cerg-action-process
env:
  APPLICATION_NAME : "CERJ2"
on: 
  push:
    branches: 
      - master

jobs:
  validating_docker:
    runs-on: self-hosted
    
    steps:
      - name: PrintAppData
        run : |
          echo "Acclication name: ${{ env.APPLICATION_NAME}}"
          
      - name: Checkout
        uses: actions/checkout@v1     

      - name: MavenVersion
        run : |
          cd i113_eureka
          ls -la
          mvn -version         
        


